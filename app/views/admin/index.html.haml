.container
  .company
    %h2 管理公司資訊
    = link_to "新增公司", new_company_path, class: "new"
    .wrap
      - @companies.each do |company|
        .card
          %h2= link_to company.title, edit_company_path(company)
          -# %p= '公司介紹'
          -# %p= company.description
          -# %p= '公司理念'
          -# %p= company.idea
          -# %p= '媒體報導'
          -# %p= company.report
          -# %p= '薪資福利'
          -# %p= company.salary
  .job
    %h2 目前職缺
    = link_to "新增職缺", new_job_path, class: "new"
    .wrap
      - @jobs.each do |job|
        .card
          %h2= link_to job.title, edit_job_path(job)



-# .container
-#   %h2 職缺列表
-#   - @jobs.each do |job|
-#     .coloum
-#       %h2= link_to job.title, job
    -# %p= '公司介紹'
    -# %p= job.description
  -# %p= '公司理念'
  -# %p= job.idea
  -# %p= '媒體報導'
  -# %p= job.report
  -# %p= '薪資福利'
  -# %p= job.salary

-# .container
-#   %h2 公司列表
-#   - @companies.each do |company|
-#     .coloum
-#       %h2= link_to company.title, company


-# - @company.each do |company|
-#   %h2= link_to company.company, companies_path(:name => company.company)